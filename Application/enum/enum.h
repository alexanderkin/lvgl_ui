#ifndef ENUM_H
#define ENUM_H

typedef enum ON_OFF {
    ON,
    OFF,
} on_off_t;

typedef enum Windows {
    FirstWindow,
    SecondWindow,
    ThirdWindow,
    EndWindow,
} windows_t;

#endif // ENUM_H